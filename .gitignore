# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# uv / virtualenv
.venv/
venv/

# Environment / secrets
.env
.env.local
.env.*.local

# Data caches / downloads
*.duckdb
*.duckdb.wal
# Exception: the built store is committed so the deployed app has data without an
# expensive rebuild (briefs cost LLM calls; Open Food Facts is flaky). ~2.3 MB.
!data/label_lens.duckdb
data/raw/
data/cache/
data/chroma/

# Notebooks
.ipynb_checkpoints/

# Test / tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/

# OS / editor
.DS_Store
.idea/
.vscode/
